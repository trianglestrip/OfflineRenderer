---
description: 本机编译环境配置，包含工具链路径、版本信息和构建命令
alwaysApply: true
---

# 编译环境配置

## 本机工具链

| 工具 | 版本 | 路径 |
|------|------|------|
| Visual Studio | 2022 Community (17.14.36202.13) | `d:\Microsoft Visual Studio\2022\Community` |
| CMake | 3.31.0 | 系统 PATH |
| CUDA Toolkit | 11.8 (项目要求 12.5，需升级) | `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v11.8` |
| OptiX SDK | 未安装 (项目要求 8.0) | 安装后补充路径 |
| GPU | NVIDIA GeForce MX550 | - |
| NVIDIA 驱动 | 581.95 | - |

## 待完成环境准备

- CUDA Toolkit 需从 11.8 升级到 12.5+，下载地址: https://developer.nvidia.com/cuda-toolkit-archive
- OptiX SDK 8.0 需安装，下载地址: https://developer.nvidia.com/designworks/optix/downloads/legacy
- 安装 OptiX 后需设置环境变量 `OptiX_INSTALL_DIR` 指向 SDK 根目录

## CMake 构建命令

配置（在项目根目录执行）:

```powershell
cmake -B build -G "Visual Studio 17 2022" -A x64
```

编译:

```powershell
cmake --build build --config Release
```

Debug 编译:

```powershell
cmake --build build --config Debug
```

## 项目依赖

- CUDA 12.5+
- OptiX 8.0 (需要 Maxwell 或更新架构的 NVIDIA GPU)
- OpenEXR 3.1 (HostProgram)
- assimp 5.0 (已作为 submodule 包含在 ext/assimp)
- GLFW (已作为 submodule 包含在 ext/glfw)
- gl3w (已作为 submodule 包含在 ext/gl3w)
