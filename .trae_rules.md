# Trae IDE 规则文件 - OfflineRenderer 项目

## 项目基本信息
- **项目名称**：OfflineRenderer
- **主要技术栈**：C++、OptiX、CUDA
- **项目类型**：离线渲染器
- **当前分支**：libOptixW

## 用户习惯和偏好

### 1. 代码风格和重构偏好
- ✅ **喜欢使用结构体封装多参数**：将多个相关参数打包成结构体，提高可读性和可维护性
- ✅ **保持向后兼容性**：重构时保留原有方法接口，不破坏现有代码
- ✅ **使用现代 C++ 特性**：偏好使用 `std::variant`、`std::visit`、`std::span` 等 C++17/20 特性
- ✅ **API 设计简洁化**：简化公共接口，提供更易用的 API

### 2. 代码组织偏好
- ✅ **按功能分类**：将相关的功能和数据组织在一起
- ✅ **PIMPL 模式**：喜欢使用 PIMPL (Pointer to Implementation) 设计模式来隐藏实现细节
- ✅ **使用 std::variant 代替多继承**：对于材质等变体类型，使用 `std::variant` 而不是继承

### 3. Git 提交规范
- ✅ **提交说明用中文**：所有 git commit message 使用中文
- ✅ **避免乱码**：注意字符编码，确保中文正确显示
- ✅ **清晰的提交信息**：简要描述变更内容

### 4. 沟通方式
- ✅ **使用中文交流**：所有对话和沟通使用中文
- ✅ **直接具体的需求**：用户会提出具体的改进建议和需求

### 5. 工作方式
- ✅ **并行执行任务**：用户喜欢让多个重构任务并行执行
- ✅ **系统分析代码库**：在开始重构前，先系统地分析整个代码库
- ✅ **逐步迭代改进**：通过多次迭代逐步完善代码

## 项目特定配置

### 目录结构
```
OfflineRenderer/
├── libOptixW/              # 主要的 OptiX 包装库
│   ├── include/optixw/     # 公共头文件
│   ├── src/                # 实现文件
│   ├── kernels/            # CUDA 内核
│   └── CMakeLists.txt
├── test/                   # 测试代码
└── gallery/                # 渲染结果画廊
```

### 关键文件
- **`libOptixW/include/optixw/types.h`** - 类型定义，包括材质参数结构体
- **`libOptixW/include/optixw/optixw.h`** - 公共 API 接口
- **`libOptixW/src/scene.cpp`** - Scene 类实现
- **`libOptixW/src/renderer.cpp`** - Renderer 类实现

### 已完成的重构
1. ✅ 添加了完整的材质参数结构体（13 种材质类型）
2. ✅ 添加了 `MaterialParams` std::variant 统一材质接口
3. ✅ 添加了 `TriangleMeshParams`、`EnvironmentMapParams`、`TextureLoadParams`、`RenderConfig` 等参数结构体
4. ✅ 添加了 `Denoiser` 和 `TextureManager` 相关的参数结构体
5. ✅ 更新了 Scene 类接口，简化为一个统一的 `addMaterial` 方法
6. ✅ 更新了 Renderer、GeometryManager、Denoiser、TextureManager 类接口
7. ✅ 在所有实现文件中添加了结构体版本的方法
8. ✅ 保持了向后兼容性

## 设计原则

### 材质数据设计
- **不建议使用继承**：MaterialData 使用 Tagged Union（标签联合体）模式，而不是继承，因为：
  - GPU 性能优先
  - 简单的内存布局，固定大小
  - 访问简单，无需类型转换
  - 缓存友好

### API 设计原则
- **提供双接口**：同时提供原有的多参数方法和新的结构体方法
- **使用 std::visit**：在实现中使用 `std::visit` 来处理 variant 类型
- **类型安全**：通过结构体提供编译时类型检查

## 下次改进建议
- 考虑将 Renderer::Impl 中的成员变量按功能分类封装成结构体
- 检查其他多参数方法是否也可以结构体化
- 考虑添加更多的示例代码，展示新 API 的使用方式

---
*最后更新：2026-03-02*
