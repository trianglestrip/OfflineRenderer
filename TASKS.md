# Development Roadmap and Task Breakdown

本文档列出了 `libOptixW` 相对于 `libVLR` 需要补齐的功能，并将其拆分为可执行的开发任务，便于通过 issue/项目板追踪。

## 1. 核心渲染功能（优先级排序）

1. **BSDF 扩展** _(高优先级)_
   - Specular BRDF/BTDF。
   - GGX/微表面模型及 Fresnel。
   - 混合材质（多层/混合系数）。
   - 设计节点化材质编辑接口 / 序列化格式。

2. **纹理与贴图** _(高优先级)_
   - 添加 2D/3D 纹理采样支持。
   - 法线贴图、高度贴图、Alpha 贴图。
   - 多通道纹理（如光谱纹理）。

3. **光谱渲染系统** _(可延后)_
   - 实现 RGB→光谱转换（Meng‑Simon 或类似）。
   - 支持蒙特卡洛光谱采样、波长追踪。
   - 数据结构与存储：光谱颜色类型、谱带纹理。

*说明*：当前 RGB 渲染满足大部分需求，全光谱渲染可作为未来扩展，不在短期迭代的高优先级列表中。

## 2. 场景与几何

4. **实例化与场景图**
   - 支持几何体实例化（transform+material override）。
   - 场景节点树接口、层级遍历/更新。
   - 动态物体与更新通知机制。

5. **高级相机模型**
   - 景深透视相机（光圈、焦距）。
   - 环境相机 / 立方体/球面映射。
   - 支持其他投影（平行、鱼眼）。

## 3. 算法与优化

6. **光线传输算法**
   - 实现 LVC‑BPT、双向路径追踪等。
   - 提供算法选择 API、参数配置。

7. **性能优化**
   - 队列压缩改进、流式采样（动态SPP）。
   - 多 GPU 协同渲染（分帧/分层）。
   - OptiX Denoiser + albedo/normal 指导。

## 4. 辅助与扩展特性

8. **光源扩展**
   - 环境光 (HDRI/IBL)。
   - 方向光、IES 光源。

9. **高级渲染特性**
   - 体积散射/吸收、参与介质。
   - 光子映射或其他偏差算法。
   - 环境光/IES 贴图支持。

10. **工具与文档**
    - 更新示例程序与测试用例。
    - 编写规范与开发指南（.mdc 文档）。
    - 维护与 libVLR 对比的功能矩阵。

---

以上任务可作为每个 GitHub issue 的模板或项目板卡片。根据优先级和依赖关系调整执行顺序，为每项任务设定负责人和预计完成时间。
