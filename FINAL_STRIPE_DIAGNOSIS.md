# Cornell Box 后墙条纹问题 - 最终诊断报告

## 问题描述
渲染的 Cornell Box 场景中，后墙（白色墙面）出现规律的对角线条纹图案。

## 系统性诊断结果

### 已排除的原因
经过详尽测试，以下因素**均已排除**：

1. ✗ **OptiX Denoiser 伪影**
   - 测试：spp=64 无降噪器
   - 结果：条纹仍存在

2. ✗ **随机数种子问题**
   - 测试：改进种子哈希算法
   - 结果：条纹仍存在

3. ✗ **Subpixel Jitter**
   - 测试：完全移除 jitter，使用像素中心采样
   - 结果：条纹仍存在

4. ✗ **多采样累积**
   - 测试：spp=1
   - 结果：条纹在单次渲染时就存在

5. ✗ **分辨率/采样频率**
   - 测试：512x512 → 1024x1024
   - 结果：条纹密度不变，仍存在

6. ✗ **三角形接缝/对角线方向**
   - 测试：改变后墙三角形分割方向（对角线从 0-2 改为 1-3）
   - 结果：条纹方向未改变

7. ✗ **顶点顺序**
   - 测试：匹配 libVLR 的顶点顺序（LB-RB-RT-LT）
   - 结果：条纹仍存在

### 条纹特征
- **位置**：仅在后墙（白色墙面）
- **形状**：规律的对角线图案（从左下到右上，约 45°）
- **一致性**：与所有渲染参数无关
- **其他墙面**：地板、天花板、左墙、右墙均无此问题

## 可能的根本原因

### 假设 1：浮点数精度问题 ⭐⭐⭐
后墙是一个完美的平面（z=-1），所有顶点的 z 坐标完全相同。这可能导致：
- 光线-三角形相交测试中的数值不稳定
- 深度缓冲精度问题
- 法线计算中的微小误差被放大

**证据**：
- 只在后墙出现（完美平面）
- 其他墙面有轻微倾斜或不同的几何配置

### 假设 2：GPU 驱动或 OptiX 8.0 的 Bug ⭐⭐
特定的几何配置（完美平面 + 特定尺寸）可能触发 GPU 驱动或 OptiX 的已知/未知 bug。

**证据**：
- 所有软件层面的修复均无效
- 条纹图案非常规律，像是硬件层面的伪影

### 假设 3：光线方向量化/舍入 ⭐
GPU 在计算光线方向时可能使用了某种量化或舍入策略，导致相邻像素的光线方向略有不同，在完美平面上产生可见的伪影。

## 对比 libVLR

### 相似之处
- 顶点顺序：已匹配
- 索引顺序：已匹配
- 材质：Lambertian diffuse（相同）

### 差异
1. **libVLR 使用 Megakernel 架构**，我们使用 Wavefront
2. **libVLR 使用光谱渲染**（SampledSpectrum），我们使用 RGB
3. **libVLR 的法线插值**包含正交性保证（我们未实现）

## 建议的解决方案

### 方案 A：微调后墙几何（推荐） ⭐⭐⭐
将后墙略微向后移动（z=-1.001），打破完美平面：
```cpp
float back[] = {
    L, B, -1.001f,  R, B, -1.001f,
    R, T, -1.001f,  L, T, -1.001f
};
```

### 方案 B：增加几何细分
将后墙分割为 4x4 或 8x8 的网格，而不是 2 个三角形。

### 方案 C：实现顶点法线插值
参考 libVLR 的实现，为每个顶点存储法线，并在命中点进行插值：
```cpp
Normal3D shadingNormal = b0 * v0.normal + b1 * v1.normal + b2 * v2.normal;
// 保证正交性
float dotNT = dot(shadingNormal, tangent);
tangent = tangent - dotNT * shadingNormal;
```

### 方案 D：接受现状
如果条纹在实际应用中不明显（特别是在有纹理或更复杂场景中），可以暂时接受。

## 间接光照问题

### 当前状态
- ✅ 间接光照已实现（cosine hemisphere sampling）
- ✅ 路径追踪逻辑正确
- ⚠️ 效果不够明显

### 原因
1. **环境光过强**：`ambient = 0.005f` 仍然掩盖了间接光照
2. **光源过强**：天花板面光源发射强度过高
3. **Russian Roulette 过早**：`depth >= 3` 时就开始 RR

### 建议修复
1. **完全移除环境光**：
```cpp
// float ambient = 0.005f;  // 删除
wpt::float3_rgb Li = wpt::make_rgb(0, 0, 0);  // 只保留直接光照和间接光照
```

2. **降低光源强度**：
```cpp
areaLight.emission = vlrw::RGB(5.0f, 5.0f, 5.0f);  // 从 10.0 降低到 5.0
```

3. **延迟 Russian Roulette**：
```cpp
if (ray.depth >= 5)  // 从 3 改为 5
```

4. **增加最大路径长度**：
```cpp
const uint32_t maxDepth = 12;  // 从 8 改为 12
```

## 下一步行动

### 立即测试
1. **方案 A**：微调后墙 z 坐标（最简单，可能有效）
2. **间接光照修复**：移除环境光，降低光源强度

### 如果方案 A 无效
1. 尝试方案 B（几何细分）
2. 检查 libVLR 的实际渲染结果，确认是否也有类似问题
3. 在 NVIDIA 开发者论坛报告可能的 OptiX bug

### 长期改进
1. 实现顶点法线插值（方案 C）
2. 添加法线贴图支持
3. 实现更复杂的材质系统（参考 libVLR）

## 参考
- libVLR Cornell Box 实现：`libVLR_reference/HostProgram/scene.cpp:528-674`
- libVLR 三角形法线计算：`libVLR_reference/libVLR/GPU_kernels/triangle.cu`
- libVLR 材质系统：`libVLR_reference/libVLR/GPU_kernels/materials.cu`
