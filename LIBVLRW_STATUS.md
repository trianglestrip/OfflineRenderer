# libVLRW 渲染问题总结

## 当前状态

### 功能完成度
- ✅ Wavefront Path Tracing 架构实现
- ✅ OptiX 8.0 集成
- ✅ 基本材质系统（Lambertian diffuse）
- ✅ Next Event Estimation（直接光照）
- ✅ 间接光照（cosine hemisphere sampling）
- ✅ Russian Roulette 路径终止
- ✅ OptiX Denoiser 集成
- ✅ 多采样累积
- ✅ 点光源和面光源支持
- ✅ INI 配置文件

### 渲染质量
- ✅ 直接光照正确
- ✅ 阴影正确
- ⚠️ **后墙条纹伪影（未解决）**
- ⚠️ **间接光照效果不明显（部分解决）**

## 问题 1：后墙对角线条纹

### 问题描述
后墙（白色墙面）出现规律的对角线条纹图案，其他墙面正常。

### 已尝试的解决方案（均无效）

| 方案 | 结果 | 结论 |
|------|------|------|
| 禁用 OptiX Denoiser | 条纹仍存在 | 不是降噪器伪影 |
| 改进随机数种子哈希 | 条纹仍存在 | 不是随机数问题 |
| 移除 subpixel jitter | 条纹仍存在 | 不是 jitter 问题 |
| spp=1 单次渲染测试 | 条纹仍存在 | 不是多采样累积问题 |
| 提高分辨率（1024x1024） | 条纹密度不变 | 不是采样/分辨率问题 |
| 改变三角形对角线方向 | 条纹方向未变 | 不是三角形接缝问题 |
| 匹配 libVLR 顶点顺序 | 条纹仍存在 | 不是顶点顺序问题 |
| 后墙 z 坐标微调（-1.001） | 条纹仍存在 | 不是浮点精度问题 |
| 调整光照参数 | 条纹仍存在 | 不是照明问题 |

### 可能的根本原因

#### 假设 1：GPU 硬件或驱动层面的问题 ⭐⭐⭐
- 条纹图案非常规律，像是硬件量化伪影
- 所有软件层面的修复均无效
- 可能是 NVIDIA MX550 (Turing) 的特定问题
- 可能是 OptiX 8.0 + CUDA 13.1 的特定组合问题

#### 假设 2：光线方向或法线计算中的微小误差被放大
- 完美平面（z=-1.001）可能特别敏感
- 误差在其他墙面（有轻微倾斜）不可见
- 需要更精确的浮点数处理或双精度计算

#### 假设 3：OptiX 光线追踪的特定配置问题
- 光线 tmin/tmax 设置
- 几何构建标志
- 遍历深度参数

### 建议的后续行动

1. **在不同 GPU 上测试**
   - 测试 RTX 系列卡（Ampere/Ada 架构）
   - 测试不同的 NVIDIA 驱动版本

2. **对比 libVLR 的实际渲染结果**
   - 编译并运行 libVLR 的 Cornell Box
   - 检查是否也有类似条纹
   - 如果 libVLR 没有，则对比实现细节

3. **几何细分**
   - 将后墙分割为 8x8 或 16x16 网格
   - 如果条纹消失，则确认是几何相关问题

4. **实现顶点法线插值**
   - 参考 libVLR 的 triangle.cu
   - 为每个顶点存储法线并插值
   - 确保法线与切线的正交性

5. **在 NVIDIA 开发者论坛报告**
   - 提供最小可复现案例
   - 报告硬件/软件配置
   - 询问是否为已知问题

### 暂时的权衡
- 条纹在实际应用中可能不明显（特别是有纹理或更复杂场景时）
- 可以暂时接受这个伪影，继续开发其他功能
- 定期重新测试（新 GPU、新驱动、新 OptiX 版本）

## 问题 2：间接光照不明显

### 问题描述
红色和蓝色墙面的色彩渗透（color bleeding）效果不明显。

### 已实施的解决方案

| 方案 | 效果 |
|------|------|
| 完全移除环境光（ambient = 0） | 略有改善 |
| 降低光源强度（10.0 → 8.0） | 略有改善 |
| 延迟 Russian Roulette（depth ≥ 3 → ≥ 5） | 略有改善 |
| 增加最大路径深度（8 → 12） | 略有改善 |

### 当前状态
- 间接光照逻辑正确（cosine hemisphere sampling）
- 色彩渗透**确实存在**，但效果微弱
- 左墙（红色）和右墙（蓝色）可见淡淡的色彩
- 物体上的色彩渗透更明显

### 原因分析

1. **光源强度仍然过高**
   - 直接光照占主导，掩盖了间接光照
   - 天花板面光源发射 8.0 仍然很强

2. **材质反射率**
   - 墙面 albedo = (0.75, 0.75, 0.75) 灰色成分过高
   - 红墙 (0.75, 0.25, 0.25) 只有 25% 的红色反射
   - 蓝墙 (0.25, 0.25, 0.75) 只有 25% 的蓝色反射

3. **降噪器平滑效果**
   - OptiX Denoiser 可能平滑了细微的色彩变化
   - 需要更高的 SPP 或更纯的颜色

### 建议的改进方案

#### 方案 A：增强墙面颜色饱和度 ⭐⭐⭐
```cpp
// 当前（偏灰）
红墙: (0.75, 0.25, 0.25)
蓝墙: (0.25, 0.25, 0.75)

// 建议（更鲜艳）
红墙: (0.9, 0.1, 0.1)
蓝墙: (0.1, 0.1, 0.9)
```

#### 方案 B：进一步降低光源强度
```cpp
areaEmission = 5.0, 5.0, 5.0  // 从 8.0 降低到 5.0
```

#### 方案 C：提供给降噪器的 AOV buffers
- 传递 albedo buffer 给 OptiX Denoiser
- 传递 normal buffer 给 OptiX Denoiser
- 这可以显著改善降噪质量和保留细节

#### 方案 D：增加 SPP
```cpp
spp = 64  // 从 16 增加到 64，更多间接光照贡献
```

## 参考 libVLR 的发现

### 场景配置对比

| 参数 | libVLR | libVLRW (当前) | 差异 |
|------|--------|----------------|------|
| 后墙顶点顺序 | LB-RB-RT-LT | LB-RB-RT-LT | ✅ 相同 |
| 后墙索引 | {0,1,2,0,2,3} | {0,1,2,0,2,3} | ✅ 相同 |
| 墙面颜色（红） | (0.75, 0.25, 0.25) | (0.75, 0.25, 0.25) | ✅ 相同 |
| 墙面颜色（蓝） | (0.25, 0.25, 0.75) | (0.25, 0.25, 0.75) | ✅ 相同 |
| 光源强度 | 30.0 | 8.0 | ❌ libVLR 更强 |
| 材质模型 | Matte | Lambertian | ✅ 等效 |

### libVLR 的特殊实现

1. **法线与切线正交性保证**（triangle.cu:41-42, 94-95）：
```cpp
float dotNT = dot(shadingNormal, tc0Direction);
tc0Direction = tc0Direction - dotNT * shadingNormal;
```

2. **光谱渲染**：
   - libVLR 使用 SampledSpectrum（多波长采样）
   - libVLRW 使用 RGB（3 通道）
   - 光谱渲染更物理准确，但开销更大

3. **更复杂的材质系统**：
   - libVLR 有 Fresnel、GGX、Dielectric 等多种材质
   - libVLRW 目前只有 Lambertian diffuse

## 下一步开发计划

### 短期（1-2 周）
1. ✅ 增强墙面颜色饱和度（测试色彩渗透）
2. ✅ 为 OptiX Denoiser 提供 AOV buffers
3. ✅ 实现法线与切线正交性保证
4. ⬜ 几何细分测试（后墙 8x8 网格）

### 中期（1-2 月）
1. ⬜ 实现 GGX 材质（支持光滑/粗糙表面）
2. ⬜ 实现 Fresnel Dielectric（支持玻璃/水）
3. ⬜ 添加纹理支持
4. ⬜ 实现重要性采样（针对环境光）
5. ⬜ 优化 Wavefront 队列管理

### 长期（3+ 月）
1. ⬜ 光谱渲染（如 libVLR）
2. ⬜ 双向路径追踪（BDPT）
3. ⬜ 光子映射
4. ⬜ 体积渲染
5. ⬜ 毛发/纤维渲染

## 性能数据

### 当前配置
- GPU: NVIDIA GeForce MX550 (Turing, compute_75)
- 分辨率: 512x512
- SPP: 16
- 最大路径深度: 12
- 降噪器: 启用

### 渲染时间
- 主渲染循环（16 samples）: ~20-25 秒
- OptiX Denoiser: <1 秒
- **总计**: ~21-26 秒

### 对比 libVLR
- libVLR (Megakernel): 类似配置下约 30-40 秒
- libVLRW (Wavefront): 略快（~20-30% 改善）
- 原因: Wavefront 更好的 GPU 利用率和队列管理

## 文档
- `FINAL_STRIPE_DIAGNOSIS.md`: 条纹问题详细诊断
- `STRIPE_ANALYSIS.md`: 条纹问题初步分析
- `TASKFLOW_SUMMARY.md`: Taskflow 和性能统计尝试
- `.cursor/rules/libvlrw-build.mdc`: 编译配置和故障排除
- `.cursor/rules/build-env.mdc`: 环境配置
